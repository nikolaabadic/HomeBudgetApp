@model HomeBudgetApp.WebApp.Models.LoginViewModel

@{
    ViewData["Title"] = "Welcome!";
}
@section Style{
    <link rel="stylesheet" href="~/css/login.css" />
}

<div class="wrapper">

    <div class="col-md-4 login">
        <h4>Welcome!</h4>
        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="Username" class="form-control field" placeholder="username" spellcheck="false" value="markronson" />
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="Password" class="form-control field" type="password" placeholder="pin" value="1234" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
